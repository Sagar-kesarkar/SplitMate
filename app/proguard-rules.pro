# Add project-specific ProGuard rules here. These rules are appended to the
# Android Gradle Plugin defaults configured in app/build.gradle.kts.

# For more details, see:
# https://developer.android.com/build/shrink-code

-keep class com.splitmate.app.data.local.** { *; }
